#!/usr/bin/env zsh

# Aliases

# `ll` is deliberately not here: it is a zsh-abbr abbreviation (see
# ../abbreviations), and one name with two definitions is one too many.
alias code='open -a /Applications/Visual\ Studio\ Code.app'